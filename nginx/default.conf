server {
    listen 80;
    server_name кардиомарфон.рф www.кардиомарфон.рф; # Или вставь xn--...

    # Бэкенд
    location /api/ {
        proxy_pass http://backend:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }

    # Фронтенд
    location / {
        proxy_pass http://frontend:80;
    }
}